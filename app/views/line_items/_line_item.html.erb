<% if line_item == @current_item %>
<tr id ="current_item">
<% else %>
<tr>
<% end %>
  <td><%= line_item.quantity %>&times;</td>
  <td><%= line_item.product.try(:title) %></td>
  <td class="item_price"><%= number_to_currency(line_item.total_price, :unit  => "£") %></td>
  <td><%= button_to 'Decrease', decrease_line_item_path(line_item), method: :push, remote: true %></td>
  <td><%#= button_to 'Delete Line Item', line_item, method: :delete, data: { confirm: 'Are you sure you want to delete this line item?'} %></td>
</tr>
